-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-timeout==2.4.0
# Optional sharding support. Public CI must not enable this without fresh,
# committed timing data; see docs/CI_TEST_PARALLELISM.md.
pytest-split==0.11.0
# Local parallel test execution. The public CI baseline is intentionally
# serial until hosted-runner measurements justify a worker-count change.
pytest-xdist==3.8.0
respx==0.23.1
ruff==0.15.20
mypy==2.1.0
types-aiofiles==25.1.0.20260518
